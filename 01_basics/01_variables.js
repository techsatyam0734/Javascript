const accountId = 101
let accountEmail = "satyam0734@example.com"
var accountPassword = 12345
accountCity = "Mumbai"

// accountId = 102 // not allowed

console.log([accountId,accountEmail,accountPassword,accountCity]);

// var - functional-scoped
// let and const - block scoped
// In const initialization and declaration must be done at the same time and cannot be reassigned a new value
// In let initialization can be done first and later on we can assign a value to it

// prefer not use var 